// Lives in @/types so the storage-info service can use the shape without importing this module.
export { StorageInfoSchema } from "@/types/storage-info";
export type { StorageInfo } from "@/types/storage-info";
