# Loaded by `vite build` (the tauri build step), on CI as well as locally.
# A Discord application id is public — it ships inside every client — so it
# lives here rather than in the ignored .env, which CI never sees.
VITE_DISCORD_CLIENT_ID=1516862913598718074
